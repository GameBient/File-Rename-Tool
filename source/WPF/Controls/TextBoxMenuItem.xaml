<MenuItem x:Class="File_Rename_Tool.Controls.TextBoxMenuItem"
          xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
          xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
          xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
          xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
          mc:Ignorable="d">
    <MenuItem.Header>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="80" />
                <ColumnDefinition MinWidth="120" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="{Binding Label, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type MenuItem}}}"
                       Grid.Column="0"/>
            <TextBox Text="{Binding Input, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type MenuItem}}}"
                     Grid.Column="1"/>
        </Grid>
    </MenuItem.Header>
</MenuItem>
